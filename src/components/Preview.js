import React from 'react';
import Header from './Header';
import {FormTitle} from '../theme/style';

const Preview = () => {
  return (
    <>
      <Header stepNow={4} />
      <main>
        <FormTitle>Preview sample paragraphs</FormTitle>
      </main>
    </>
  );
};

export default Preview;
